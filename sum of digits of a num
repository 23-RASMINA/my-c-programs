#include<stdio.h>
	int sum(int n)
	{
		if(n==0)
		return 0;
		return (n%10+sum(n/10));
	}
	int main()
	{
		int num,result;
		printf("enter the number:");
		scanf("%d",&num);
		result=sum(num);
		printf("the sum of digits of the number is:%d",result);
		return 0;
	}
